ARG DOCKER_OS_IMAGE=
ARG DOCKER_OS_TAG=
FROM ${DOCKER_OS_IMAGE}:${DOCKER_OS_TAG}
ARG BUILDER_DIR=/build
RUN mkdir -p ${BUILDER_DIR}
COPY ./ ${BUILDER_DIR}
RUN cd ${BUILDER_DIR} && \
    ./install.sh && \
    ./setup.sh
RUN rm -rf ${BUILDER_DIR}
ENTRYPOINT "__INIT_SCRIPT__"
